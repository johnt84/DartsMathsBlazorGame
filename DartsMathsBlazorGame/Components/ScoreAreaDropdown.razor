<MudSelect T="ScoreArea"
           ReadOnly="ReadOnly"
           Value="ScoringArea"
           Label="@Label"
           Class="@Class"
           ValueChanged="ScoringAreaChanged"
           Placeholder="Please Select"
           Adornment="Adornment"
           AdornmentIcon="@AdornmentIcon"
           AdornmentColor="AdornmentColor">
    @foreach (var scoreArea in ScoreAreas)
    {
        <MudSelectItem Value="@scoreArea">
            @scoreArea
        </MudSelectItem>
    }
</MudSelect>
